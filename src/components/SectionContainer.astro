---
const { class: className, id, ...attributes } = Astro.props
---

<section
  {...attributes}
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full mx-auto container lg:max-w-4xl md:max-w-2xl`}
>
  <slot />
</section>
